import './core';
import './alpine';

/**
 * Entry point for the customer-facing storefront. jQuery + the legacy AIZ
 * compat shim load classically, before this module runs — see
 * components/legacy-js-bridge.blade.php.
 */
